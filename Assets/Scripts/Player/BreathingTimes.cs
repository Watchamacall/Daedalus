using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BreathingTimes : MonoBehaviour
{
    //public AudioManager audioWithin;
    //[Header("General Settings")]
    //[Tooltip("The enemy GameObject")]
    //public GameObject enemy;

    //[Tooltip("The name of the clip which you want to add here")]
    //public string slowHeartRate, mediumHeartRate, fastHeartRate;

    //[Tooltip("The distance for the medium heart rate to play")]
    //public float distanceToMedium;
    //[Tooltip("The distance for the fast heart rate to play")]
    //public float distanceToFast;

    //float distance;

    //Sound currentSound;

    //// Update is called once per frame
    //void Update()
    //{
    //    distance = Vector3.Distance(transform.position, enemy.transform.position);

    //    if (distance < distanceToMedium && !currentSound.source.isPlaying)
    //    {
    //        currentSound = audioWithin.GetSound(mediumHeartRate);
    //        currentSound.source.Play();
    //    }
    //    else if (distance < distanceToFast && !currentSound.source.isPlaying)
    //    {
    //        currentSound = audioWithin.GetSound(fastHeartRate);
    //        currentSound.source.Play();
    //    }
    //    else
    //    {
    //        if (!currentSound.source.isPlaying)
    //        {
    //            currentSound = audioWithin.GetSound(slowHeartRate);
    //            currentSound.source.Play();
    //        }
    //    }
    //}
}
